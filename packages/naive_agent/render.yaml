projects:
- name: kuppharish-agents-workshop-naive
  environments:
  - name: production
    databases:
    - name: kuppharish-naive-agent-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: web
      name: kuppharish-naive-agent
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-naive-agent
      startCommand: uv run python -m naive_agent.server
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kuppharish-naive-agent-db
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
